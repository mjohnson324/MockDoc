import React from 'react';

const HomeText = () => {
  return(
    <p className="home-text">
      A demo site for booking appointments for free healthcare. None of the doctors are real, and not all of them are qualified to work as doctors.
    </p>
  );
};

export default HomeText;